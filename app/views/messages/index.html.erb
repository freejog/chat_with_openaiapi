<%= form_with(model: @message, id: 'generation-form') do |form| %>
  <div class="textarea-with-submit-inside">
    <%= form.text_area :prompt,class: "rounded-corners" %>
    <%= form.submit 't', class: "submit-within-textarea rounded-corners" %>
  </div>
<% end %>